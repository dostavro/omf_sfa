
omf_sfa_am:
  name: urn:xxx

  endpoints:
    -
      type: xmlrpc
      port: 8001
      ssl:
        cert_chain_file: ~/.omf/am-cert.pem
        private_key_file: ~/.omf/am-key.pem
      trusted_roots: ~/.omf/trusted_roots
    -
      type: xmpp
      user: am_mgr-1.0
      password: pw
      server: localhost
      auth:
        :entity_cert: ~/.omf/omf_am.pem
        :entity_key: ~/.omf/omf_am_key.pem
        :root_cert_dir: ~/.omf/trusted_roots
